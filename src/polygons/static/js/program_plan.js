function display_remove_course_popup(program_plan_id, subject_id) {
   /*
      * Center the pop up window
      * Set the overflow-y of the body to hidden
      * Make the AJAX call to populate the list of dependent subjects
      * Set the subject input value using the received subject_id
      * Display the popup window container
   */
}
