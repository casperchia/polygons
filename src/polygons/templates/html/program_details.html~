{% load staticfiles %}
<!doctype html>
<html>
   <head>
      <meta charset="utf-8">
            <link href="{% static "css/base.css" %}" rel="stylesheet" type="text/css">
      <title>
         Test
      </title>
   </head>
   <body>
    {% if program %}
        <u1>
        <!-- Listing all the program details -->
        <p>Program Code: {{program.code}}</p>
        <p>Program Name: {{program.name}}</p>
        <p>Offered By: {{program.offered_by.name}}</p>
        <p>Career: {{program.career.name}}</p>
        {% if program.degree %}
            <p>Degree Name: {{program.degree.name}}</p>
        {% endif %}
        <li><a href="http://www.handbook.unsw.edu.au/{{program.career.name}}/programs/2013/{{program.code}}.html"> UNSW Handbook </a></li>
        
        <p><h2> Subjects per rule </p></h2>
        {% for r in rules %}
            <p>{{r.rule.name}}</p>
            {% for s in subjects %}
                {% if forloop.counter0 == forloop.parentloop.counter0 %}
                    {% for sub in s %}
                        <p> &nbsp; &nbsp; &nbsp; {{sub.subject.code}} </p>
                    {% endfor %}
                {% endif %}
            {% endfor %}
        {% endfor %}
        </u1>
    {% else %}
        <p> No program listed </p>
    {% endif %}
   </body>
</html>
